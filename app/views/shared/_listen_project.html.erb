  <% @projects.each do |p| %>
    <div class="well">
     <section>
      <p>
        <%= link_to p.title, project_path(p) %>
      </p>
      <%= content_tag :p, p.description %>
      <%= content_tag :p, p.deadline_submission %>
    </section>
    </div>
<% end %> 
