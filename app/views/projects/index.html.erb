<div class="container">
  
  <aside>
    <% if @projects.empty? %>
      <p>Ops, vocẽ ainda não cadastrou projetos na plataforma</p>
      <p>Cadastre seu primeiro projeto agora</p>
      <%= link_to "Novo Projeto", new_project_path, :class => "btn btn-info" %>
    <% end %>
    <%# usar class text-alert do bootstrap aqui%>
  </aside>
  <%= render '/shared/listen_project', object: @projects %>
  <%= link_to 'Voltar', root_path %>
</div>
