<div class="container">
  
  <aside>
    <% if @projects.empty? %>
      <p>Ops, vocẽ ainda não cadastrou projetos na plataforma</p>
      <p>Cadastre seu primeiro projeto agora</p>
      <%= link_to "Novo Projeto", new_project_path, :class => "btn btn-info" %>
    <% end %>
    <%# usar class text-alert do bootstrap aqui%>
  </aside>
  <%= render '/projects/views/user_project', object: @projects %>
  <%= link_to 'Voltar', root_path, class: "bg-blue-500 border-2 border-blue-500 text-white rounded-lg text-center px-3 py-1 w-24
             transition-colors duration-700 hover:bg-white hover:border-2 hover:border-blue-500 hover:text-gray-700" %>
</div>
