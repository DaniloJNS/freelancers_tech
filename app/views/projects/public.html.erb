
<div class="container">
  <aside>
    <% if @projects.empty? %>
    <p>Ops, no momento não temos projetos abertos</p>
    <p>Tente novamente mais tarde</p>
    <% end %>
    <%# usar class text-alert do bootstrap aqui%>
  </aside>
  <% @projects.each do |p| %>
    <section>
      <p>
        <%= link_to p.title, project_path(p) %>
      </p>
      <%= content_tag :p, p.description %>
      <%= content_tag :p, p.deadline_submission %>
    </section>
  <% end %> 
</div>
<%= link_to 'Voltar', root_path %>
